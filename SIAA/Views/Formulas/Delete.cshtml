@model SIAAPI.Models.PlaneacionProduccion.Formula
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>¿Está seguro de borrar este registro?</h3>
<div>
    <h4>Eliminar Fórmula</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IDProceso)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IDProceso)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IDTipoArticulo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FormulaP)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-danger btn-xs" /> |
            @Html.ActionLink("Regresar a la lista", "Index", null, new { @class = "btn btn-default btn-xs" })
        </div>
    }
</div>
